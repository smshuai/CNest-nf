docker.enabled = true

params {
    project = false
    bed = false
    ref = false
    design = false
    test = false
}

process {
  cpus = 1
  memory = "4.GB"
  container = "smshuai/cnest:dev"
  errorStrategy = { task.exitStatus in [403, 401] ? 'ignore' : 'terminate' }
}